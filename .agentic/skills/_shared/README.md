# 共通 skill ルール

これらのファイルはすべての `memory-*` skill が読みます。

- `SCHEMA.md`: 正規 frontmatter と file interface。
- `GUARDRAILS.md`: 共有安全性、クライアント分離、conflict handling。
- `COLLABORATION.md`: 複数人作業、Issue 中心の提案受付、push 後確認のルール。

アダプタ内でこれらのルールを重複させないでください。
